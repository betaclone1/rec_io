-- Down: schema historical_data is shared (strike_table_master etc). Do not DROP SCHEMA.
-- Dynamic KXBTC15M-* cycle tables are dropped by the packager after export; no static DDL to reverse.

SELECT 1;
