# Test and lint (install with: pip install -r requirements-dev.txt)
-r requirements.txt
pytest>=7.0
pytest-cov>=4.0
ruff>=0.1.0

# Optional for cycle_recon Parquet output (local analysis)
pyarrow>=17,<20
