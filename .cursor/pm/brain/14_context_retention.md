# Context retention across chats

Cursor does not retain chat context between sessions. This memory is how we preserve continuity. **Write things down.** Memory docs (including 14 and 15) are **for the PM's use first**—be as technical and detailed as necessary; human readability is not required. Storage is cheap; prefer writing too much over losing context.

**Default:** If we talk about it, assume the CEO wants the PM to remember it. Record it in memory (appropriate numbered doc or 14 as catch‑all). Do not wait to be asked.

**When in doubt, add it:** If you wonder whether something should go in memory, add it. Prefer writing too much over losing context.

**When the CEO says "check X":** Actually run the check (commands, read files, inspect logs, query DB, etc.). Do not reply with instructions for the CEO to check it themselves.

**/verify-local** and **/verify-production:** When the user invokes verify-local (or verify-production), run the verification workflow on the chosen server (local or prod via SSH). See .cursor/pm/VERIFY_COMMAND.md and .cursor/commands/verify-local.md, verify-production.md.

**Chat summary log (15):** The PM may update .cursor/pm/brain/15_chat_summary_log.md on its own whenever it would be helpful. **/log-chat** is the user's tool to explicitly request an update: summarize the current chat and append a dated, timestamped entry (newest at top). See .cursor/pm/LOG_CHAT_COMMAND.md.

**Questions are not directives:** If the CEO asks a question (e.g. "what's next?"), that is not an instruction to proceed with a task. Answer the question (e.g. describe the next task from the central backlog in 13_proposed_tasks). When they want you to proceed they will say so.

## What to record in memory

Record anything that a future session (or the CEO) would need to know and that would otherwise live only in chat history:

- **Decisions** — What was decided, by whom, and any conditions (e.g. "CEO approved X; do Y only after Z").
- **Outcomes** — What was done, what was deferred, what failed and why.
- **Proposed task lists / next steps** — In 13_proposed_tasks.md; when the PM suggests a list of starting tasks or next steps, put it there.
- **Open questions / blockers** — Questions still unanswered; blockers and what would unblock them; who owns the next step.
- **Handoff notes** — "Next session: continue with X; avoid Y; check Z."
- **Key facts from conversation** — Conventions the CEO stated, preferences, constraints, or context that would otherwise be lost (e.g. "Slack is primary comms; PM replies guaranteed only in IDE").
- **Ongoing work** — What’s in progress, what’s blocked, what’s waiting on the CEO.
- **Procedures and commitments** — If the PM states a procedure or commitment in chat (e.g. "going forward I will maintain both TODO and MASTER_CHANGELOG"), write it into memory (06 for conventions, 05 for changelog workflow, or the relevant doc). Stating it in chat alone is meaningless; without persistent memory it will be forgotten. Add to 06, 05, or 14 as appropriate so future sessions follow it.

Use the appropriate numbered memory doc when there’s a clear home (e.g. 13 for task lists, 07 for audit findings, 06 for conventions). Use this doc (14) as a catch‑all for **recent context / handoff** when it doesn’t fit elsewhere, or to append a short "last session" summary so the next load has immediate continuity.

## Recent context / handoff

*(Append below when something doesn’t fit elsewhere or when ending a session with important context.)*

**2026-03-09 — Prod maintenance from local (SSH):** Verify-production and daily-briefing prod health checks run from local via SSH. Apply-update is **not** run from local via SSH: **current practice is to run apply-update with an agent on the prod server** (git pull, migrations, MASTER_RESTART, verify on prod). No git push/pull from local through terminal or SSH. PROD_MAINTENANCE_FROM_LOCAL.md describes a future/optional approach (apply-update from local via SSH, fidelity check); APPLY_UPDATE_COMMAND, CHANGELOG_AGENT_INSTRUCTIONS, and apply-update skill state current practice and point to that doc as optional future.

**2026-03-08 — Redis platform initiative (draft):** CEO asked to draft plans for a full-scale Redis system to replace the internal HTTP broadcast mesh and slim main.py. Plan written in REDIS_PLATFORM_INITIATIVE.md: current state (main as hub, cross-service HTTP, no shared cache), vision (Redis pub/sub + optional cache), scope (main, trade_manager, ATS, AES, monitor_manager, kalshi_account_sync, frontend WS), phases (research/pilot/migration/main slim-down), risks, agent implications (possible @infra/@redis or extend @db). Not scheduled; when we set a timeline we refine and consider new agents. Index and 13 updated.

**2026-03-08 — OpSec push:** Push to prod when dev has been observed for a while; no fixed timeline.

**2026-03-08 — Env conventions (opportunistic cleanup):** Critical part is done (database.py single source; supervisor injects all three). When touching a file for other development, if it still uses POSTGRES_* or its own DB config, switch it to get_postgresql_connection() / get_database_config() from backend.core.config.database. Do not propose a dedicated full pass unless it seems to be becoming a bigger problem (e.g. causing bugs, confusion, or many files piling up); then flag and we can do a full pass.

**2026-03-08 — OpSec remediation completed.** All audit fixes applied: database.py requires DB password in production; main.py uses get_postgresql_connection/get_database_config throughout, local_dev_ restricted to non-prod, bcrypt required, CORS no "*" in prod, change_password and get_user_credentials use config; kalshi_account_sync_ws, auto_entry_supervisor, active_trade_supervisor, kalshi_market_ticker_websocket use centralized DB config; MASTER_RESTART, simple_deploy, first_boot_sanitize, generate_unified_supervisor_config use env; password prints removed from setup_auth.py and install.py (archive). bcrypt added to requirements.txt. **For prod push:** MASTER_CHANGELOG has entry "2026-03-08 — OpSec remediation" with Production agent checklist; .cursor/pm/OPSEC_AUDIT_AND_UPGRADE.md has section "Production server: OpSec update (2026-03-08)" with full instructions. Prod agent must ensure DB_PASSWORD or REC_DB_PASS is set on prod, install deps (bcrypt), then MASTER_RESTART and verify.

- **2026-03-08 — March 12 Kalshi:** We are already switched to _fp/_dollars. Goal is to observe that deprecation doesn't break anything (orders, fills, positions, market data); no code change needed unless something breaks.
- **2026-03-08 — First production apply-update debrief:** Apply-update ran on prod: migration 20260307_1600 applied, MASTER_RESTART, verify, backfill (9/12 account_history rows). Confirm-update run produced UPDATE CONFIRMED commit message; 19 files staged. **Sync:** After push from the env where confirm-update was run, run `git pull` on dev (and any other env) so codebase and docs match. Optional MASTER_CHANGELOG items (DO MCP on prod, post–March 12 fixed-point check) remain unchecked; no blocker. Issues to work on: main_app DeprecationWarning; 3 legacy account_history rows NULL; strike "No event_ticker" if it persists post-DST.
- **2026-03-08 — main_app DeprecationWarning (non-critical):** /verify reported ⚠️ Investigate: main_app logs show DeprecationWarning for `@app.on_event("startup")` and `@app.on_event("shutdown")` in backend/main.py (lines 4429, 4434). FastAPI wants a lifespan context manager instead. **Not critical** — app runs; fix when touching that code or before a FastAPI upgrade. Recommended: replace with async lifespan generator, pass to FastAPI(lifespan=...). Also: "Invalid HTTP request received" in main_app is external probes; harmless.
- **2026-03-08 — account_history backfill (fixed and run):** Added to kalshi_account_sync_ws.py: `_v1_request`, `fetch_v1_deposits_page`, `fetch_v1_withdrawals_page`, `_backfill_account_history_vendor_rail`, `_refresh_transfer_from_to_from_account_history`. API uses **amount_cents** and **created_ts** (not amount/created_at); backfill matches by (entry_type, amount, created within 2s). Backfill script uses backend.core.config.database.get_postgresql_connection (server-agnostic). Ran backfill on prod: 9/12 rows now have kalshi_id/vendor/rail; 3 rows remain NULL (likely legacy rows not matching current API response). Same codebase and script work on any server with same credentials layout and DB config.
- **2026-03-07:** CEO confirmed this memory is the PM’s persistent context across chats (not just task lists). Mandate: write things down liberally—decisions, outcomes, open questions, handoff notes, key facts—so we don’t lose context between sessions. Storage is cheap; prefer writing too much over losing context. Added 14_context_retention; updated INDEX and PM rule accordingly.
- **2026-03-07:** If we talk about it, assume the CEO wants it remembered. Record in memory by default; do not wait to be asked.
- **2026-03-07:** Task 1 (Kalshi fixed-point migration) completed. All Kalshi-facing code prefers _fp and _dollars; legacy derived when API omits. trade_executor, kalshi_account_sync_ws, kalshi_market_watchdog, live_orderbook_snapshot, kalshi_market_ticker_websocket updated. MASTER_CHANGELOG and 11 updated.
- **2026-03-07:** Post–MASTER_RESTART verification: main_app :3000 and trade_executor :8001 health 200; kalshi_account_sync positions/fills/orders/settlements/balance sync and DB writes OK; kalshi_market_watchdog_15m_btc saving markets to PostgreSQL. No fixed-point–related errors in logs.
- **2026-03-07:** If you wonder whether to add something to memory, add it. Prefer writing too much over losing context.
- **2026-03-08:** CEO shared a note on G Drive (REC_IO / Cursor — "Cursor Notes") with items to work on: /prepare-update command, Frontend agent onboarding, optional Backend master, /daily-briefing command, MCP investigation, OpSec audit + agent, use org structure efficiently. Full list and suggested order in 13_proposed_tasks.
 - **2026-03-10 — Central backlog convention:** 13_proposed_tasks.md is the central, prioritized backlog. When the CEO asks "what's next?" or similar, answer from the merged list (13 + TODO.md + current Cursor Notes), giving 5–6 top items (short, medium, long term). New tasks from chat, daily briefing, or Drive should be folded into 13 with priority adjusted; completed tasks should be marked done there (and in TODO.md if relevant).
- **2026-03-09:** Completed Drive tasks from Cursor Notes: (1) Org chart diagram — created REC.IO Agent Org Chart.txt in REC_IO/Cursor (current + proposed agents, structure, departments). (2) Mission statement — created rec.io Mission Statement.txt in REC_IO/Cursor (trading system for prediction markets, Kalshi, live/simulated, reliable and maintainable). Both via scripts/gdrive/create-doc.js --text-file --folder "Cursor".
- **2026-03-08 — Strike table "No event_ticker":** Do not mention in priorities unless (a) it persists as an issue over the next week, or (b) it becomes a critical operational issue. Same for 15m_eth format-string NoneType: fix when touching that path; do not proactively flag.
- **2026-03-07:** Task 2 (fix known bugs) completed: auto_entry_supervisor get_port("main_app"); main.py get_trade_history_preferences_postgresql() uses get_postgresql_connection().
- **2026-03-07:** Top-level project rules added (.cursor/pm/PROJECT_RULES.md; 06_conventions_insights): (1) Server agnostic—everything runs identically locally and on any remote server, self-contained; no hardcoded hostnames/paths. (2) No fallbacks or defaults for required data—real money; if a value is needed and missing, fix the problem; do not add fallbacks to avoid errors; missing data must fail or require config, not silent default. (3) Git tracking: necessary new files (passing op-sec) tracked; agents must not push/pull via GitHub without explicit CEO authorization; CEO determines when commits are warranted.
- **2026-03-07:** Memory docs (including 15_chat_summary_log) are first and foremost for the PM's use. Be as technical and detailed as necessary; human readability is not required.
- **2026-03-07:** Reversible DB migrations: .cursor/pm/DB_REVERSIBLE_MIGRATIONS.md, runner scripts/db/run_migration.py (list / up / down), migrations in scripts/migrations/ (YYYYMMDD_HHMM_slug.up.sql + .down.sql). PG expert agent (when onboarded) must create up+down for schema changes and use runner so DB changes can be reverted like code. 03_db_schema_brain and changelog TODO updated.
- **2026-03-07:** PM/agent docs moved into **.cursor/pm/** (CEO request: avoid sprawl in main docs/). Memory lives in .cursor/pm/brain/; command refs, ORG_CHART, PROJECT_RULES, DB_REVERSIBLE_MIGRATIONS in .cursor/pm/. References updated; docs/pm_brain/ and the six agent docs removed from docs/. Renamed from .cursor/room/ to .cursor/pm/ (CEO: "room" was just a metaphor).
- **2026-03-07:** **@db** agent onboarded. Rule: .cursor/rules/db.mdc. AGENTS.md and ORG_CHART updated. Scope: schema, reversible migrations, reference alignment, task 3 (DB maintenance audit). Next: invoke @db for task 3 or any schema/migration work.
- **2026-03-07:** CEO corrected PM: user asked "can you delegate that task directly to the new agent, right?" and PM proceeded to delegate (mcp_task). **Questions are not directives.** PM rule strengthened: cardinal-rule section at top of pm.mdc ("A question is not an instruction to proceed"; do not run tools or execute on the basis of a question alone; proceed only when user explicitly says to).
- **2026-03-07:** Task 3 (DB maintenance audit) delegated to @db via mcp_task. Completed: audit findings in docs/changelog/DB_MAINTENANCE_AUDIT_FINDINGS.md; update_db_schema_to_reference.py uses get_postgresql_connection() and env; new scripts/check_db_schema_drift.py (critical tables, exit 1 on drift); TODO.md checklist and prod plan updated; reference doc and 03_db_schema_brain updated. Deferred: prod DDL (maintenance window only); database.py CREATE TABLE alignment for trades_0001 (prefer reversible migrations first); reference format tweaks for audit/drift script. Recommendation: add check_db_schema_drift.py to CI. Done: .github/workflows/db-schema-drift.yml added (runs on push/PR to main and master).
- **2026-03-07:** CEO: any DB changes (any process) must be monitored by head of DB operations (@db); all associated files and docs updated so DB changes are painless across servers. db.mdc updated: @db is "head of DB operations", mandate to monitor all DB changes and keep in sync — reference doc, database.py, migrations, memory (03), changelog, drift check. Same in AGENTS.md, ORG_CHART, 06, DB_REVERSIBLE_MIGRATIONS.md.
- **2026-03-07:** Task 4 (Kalshi account history): migrated to /deposits and /withdrawals. DB: reversible migration 20260307_1600_account_history_vendor_rail_kalshi_id (kalshi_id, vendor, rail); reference doc and database.py updated. Sync: fetch_v1_deposits_page, fetch_v1_withdrawals_page; _deposit_to_row, _withdrawal_to_row; upsert by kalshi_id. Transfers: from/to derived from vendor/rail. Frontend: Status column; From/To show new values. API get_transfers includes status. TODO task 4 checklist and milestones updated.
- **2026-03-07:** CEO: when critical scripts (supervisor services, main_app) are touched, always call out that a restart is required and which services. Added to 06_conventions_insights ("Restart required (mandatory)") and pm.mdc (Communication bullet). No implied restart; state it explicitly.
- **2026-03-07:** CEO: when you say things like "going forward I'll do X," you have to add them to persistent memory or it's meaningless—you will just forget. Added 06 "Changelog maintenance (mandatory)": for production-relevant work, maintain both TODO.md (backlog/checklists) and MASTER_CHANGELOG.md (new dated entry with summary + prod checklist when release is ready); do not state procedures in chat without writing them into memory. Added 14 bullet: procedures and commitments stated in chat must be written into memory or they are not retained.
- **2026-03-08:** **Google Drive default location:** When the user says they've put something for the agent on Drive, look in **REC_IO / Cursor** first unless they specify another folder. Anticipated heavy use; document in Drive setup and 12.
- **2026-03-08:** Terminology: stop referring to "PM Brain" or "the brain"; use "memory" or "context" (e.g. "memory docs", "refresh context from memory", "record in memory"). Folder path .cursor/pm/brain/ and file names unchanged. INDEX, 14, AGENTS.md, pm.mdc, LOG_CHAT, README, 00, 03, 05, 06, 07, 09, 13, 15, ORG_CHART updated.
- **2026-03-08:** Google API integrations documented in .cursor/pm/GOOGLE_DRIVE_MCP_SETUP.md (retitled from "Google Drive MCP setup" to "Google API integrations"). **Enabled in GCP:** Drive, Docs, Sheets (in use); Gmail API and Calendar API enabled for future use (no scripts yet). MCP: read (search, gdrive:///). Scripts: create-doc.js (Docs/.txt, delete, auth), create-account-history-sheet.js (users.account_history_0001 → new Sheet in Cursor), export_account_history.py (DB→CSV). Default for "write a doc" = Google Doc (no --text-file). REC_IO/Cursor folder. See INDEX, 12, scripts/gdrive/README.md.
- **2026-03-07:** CEO: finish local component of DB audit; production server elements are part of the update process—@updater coordinates and verifies when pushing to prod. Local alignment completed: type-compatibility buckets in normalize_type_for_compare (STRING, NUMERIC, INT); drift check only requires database.py columns to exist in reference (reference may have extra); parser skips comment lines (--); reference updated (bankroll_allotment, max_probability). check_db_schema_drift.py passes. TODO.md and 13_proposed_tasks updated; prod handled by updater.

---

*Last updated: 2026-03-07.*
